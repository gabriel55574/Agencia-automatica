---
phase: 09-feedback-loop
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/actions/pipeline-reset.ts
  - src/components/clients/cycle-badge.tsx
  - src/components/clients/reset-pipeline-dialog.tsx
  - src/components/squad/PromptPreviewModal.tsx
  - src/app/(dashboard)/clients/[id]/page.tsx
autonomous: false
requirements:
  - FEED-01
  - FEED-02
  - FEED-03

must_haves:
  truths:
    - "Operator can click 'Reset Pipeline (New Cycle)' on a completed client profile and the client returns to Phase 1 with cycle_number incremented"
    - "Client profile shows a 'Cycle N' badge next to the client name when cycle_number >= 2"
    - "Prompt preview modal displays a 'Feedback from Previous Cycle' section with NPS/CLV/churn insights when feedbackContext is non-empty"
    - "Pipeline reset only works when client has completed Phase 5"
    - "After reset, the client profile shows new pending phases/processes/gates while old squad_jobs remain accessible"
  artifacts:
    - path: "src/lib/actions/pipeline-reset.ts"
      provides: "resetPipelineAction Server Action"
      exports: ["resetPipelineAction"]
    - path: "src/components/clients/cycle-badge.tsx"
      provides: "CycleBadge component for client profile"
      exports: ["CycleBadge"]
    - path: "src/components/clients/reset-pipeline-dialog.tsx"
      provides: "ResetPipelineDialog confirmation dialog component"
      exports: ["ResetPipelineDialog"]
    - path: "src/components/squad/PromptPreviewModal.tsx"
      provides: "Updated modal with feedback context section"
      contains: "feedbackContext"
    - path: "src/app/(dashboard)/clients/[id]/page.tsx"
      provides: "Updated profile with CycleBadge and ResetPipeline button"
      contains: "CycleBadge"
  key_links:
    - from: "src/lib/actions/pipeline-reset.ts"
      to: "reset_pipeline_cycle RPC"
      via: "admin.rpc('reset_pipeline_cycle')"
      pattern: "reset_pipeline_cycle"
    - from: "src/app/(dashboard)/clients/[id]/page.tsx"
      to: "src/components/clients/cycle-badge.tsx"
      via: "import CycleBadge"
      pattern: "CycleBadge"
    - from: "src/components/squad/PromptPreviewModal.tsx"
      to: "AssembledContext.feedbackContext"
      via: "conditional render of feedback section"
      pattern: "feedbackContext"
---

<objective>
Wire the feedback loop UI: pipeline reset button, cycle badge, and feedback context display in prompt preview.

Purpose: Give the operator the controls to start a new cycle for returning clients and see feedback context flowing into squad prompts. This completes the learning loop that makes each iteration smarter.

Output: resetPipelineAction Server Action, CycleBadge component, ResetPipelineDialog, updated PromptPreviewModal with feedback section, updated client profile page, DB push verification.
</objective>

<execution_context>
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-feedback-loop/09-CONTEXT.md
@.planning/phases/09-feedback-loop/09-01-SUMMARY.md

@src/lib/actions/gates.ts
@src/lib/actions/squad.ts
@src/app/(dashboard)/clients/[id]/page.tsx
@src/components/squad/PromptPreviewModal.tsx
@src/components/clients/archive-dialog.tsx
@src/lib/database/types.ts
@supabase/migrations/00009_feedback_loop.sql

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From src/lib/squads/feedback.ts (created in Plan 01):
```typescript
export async function extractFeedbackContext(
  clientId: string,
  supabaseClient?: SupabaseClient
): Promise<string>
```

From src/lib/squads/assembler.ts (updated in Plan 01):
```typescript
export type AssembledContext = {
  briefing: string
  priorOutputs: ProcessOutput[]
  feedbackContext: string  // NOW populated for cycle 2+ clients
  truncated: boolean
  totalOutputsAvailable: number
  outputsIncluded: number
}
```

From src/lib/database/types.ts (updated in Plan 01):
```typescript
reset_pipeline_cycle: {
  Args: { p_client_id: string }
  Returns: void
}
```

From src/lib/actions/gates.ts (pattern to follow for Server Action):
```typescript
export async function approveGateAction(
  gateId: string,
  clientId: string
): Promise<ActionResult>
```

From src/lib/actions/clients.ts:
```typescript
export type ActionResult = { success: true } | { error: string }
```

From src/components/clients/archive-dialog.tsx (pattern for confirmation dialog):
// Uses shadcn AlertDialog with trigger button, description, and confirm/cancel actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resetPipelineAction, CycleBadge, ResetPipelineDialog, update PromptPreviewModal, and wire client profile</name>
  <files>src/lib/actions/pipeline-reset.ts, src/components/clients/cycle-badge.tsx, src/components/clients/reset-pipeline-dialog.tsx, src/components/squad/PromptPreviewModal.tsx, src/app/(dashboard)/clients/[id]/page.tsx</files>
  <read_first>
    - src/lib/actions/gates.ts (Server Action pattern: auth check, Zod validation, admin.rpc call, revalidatePath)
    - src/lib/actions/clients.ts (ActionResult type import)
    - src/components/clients/archive-dialog.tsx (confirmation dialog pattern with AlertDialog)
    - src/components/squad/PromptPreviewModal.tsx (current modal structure -- add feedback section)
    - src/app/(dashboard)/clients/[id]/page.tsx (current profile page -- add CycleBadge and reset button)
    - src/lib/database/types.ts (confirm reset_pipeline_cycle in Functions section from Plan 01)
    - supabase/migrations/00009_feedback_loop.sql (understand what the RPC does)
  </read_first>
  <action>
    **Part A: Create `src/lib/actions/pipeline-reset.ts`**

    Follow the exact pattern from `gates.ts`:
    1. `'use server'` directive at top.
    2. Import: `z` from zod, `revalidatePath` from next/cache, `createClient` from server, `createAdminClient` from admin, `ActionResult` from clients.
    3. Zod schema: `z.object({ clientId: z.string().uuid() })`.
    4. Export `async function resetPipelineAction(clientId: string): Promise<ActionResult>`.
    5. Auth check: `const supabase = await createClient()` + `supabase.auth.getUser()`. Return `{ error: 'Unauthorized' }` if no user.
    6. Validate input with Zod.
    7. Call RPC: `const { error } = await (admin.rpc as any)('reset_pipeline_cycle', { p_client_id: clientId })`.
       Use `as any` cast same as gates.ts (generated types may not include the new RPC until types are regenerated).
    8. On error: `console.error('[resetPipelineAction]', error)` and return `{ error: error.message }`.
    9. On success: `revalidatePath(\`/clients/${clientId}\`)` and return `{ success: true }`.

    **Part B: Create `src/components/clients/cycle-badge.tsx`**

    A simple client component per D-09:
    ```typescript
    'use client'
    import { Badge } from '@/components/ui/badge'

    interface CycleBadgeProps {
      cycleNumber: number
    }

    export function CycleBadge({ cycleNumber }: CycleBadgeProps) {
      if (cycleNumber <= 1) return null
      return (
        <Badge variant="outline" className="text-xs font-medium border-blue-300 text-blue-700 bg-blue-50">
          Cycle {cycleNumber}
        </Badge>
      )
    }
    ```
    Cycle 1 renders nothing (first pass, no badge). Cycle 2+ shows a prominent blue badge.

    **Part C: Create `src/components/clients/reset-pipeline-dialog.tsx`**

    Create as a client component:
    - Use the same AlertDialog pattern from `archive-dialog.tsx`.
    - Trigger button text: "Reset Pipeline (New Cycle)".
    - Button only visible when ALL phases are completed (check if current_phase_number is 5 AND phase 5 status is 'completed'). Pass a `canReset` boolean prop.
    - Confirmation text: "This will start a new pipeline cycle for {clientName}. All existing squad outputs will be preserved. The client will return to Phase 1 (Diagnostico)."
    - On confirm: call `resetPipelineAction(clientId)`. Show toast on success/error. Use `useRouter().refresh()` on success.
    - Props: `clientId: string`, `clientName: string`, `canReset: boolean`.
    - When `canReset` is false, render the button as disabled with a tooltip: "Client must complete Phase 5 before starting a new cycle".

    **Part D: Update `src/components/squad/PromptPreviewModal.tsx`**

    Add a "Feedback from Previous Cycle" section per D-10:
    1. After the "Prior Outputs" section and before the "Full Prompt" section, add a conditional block:
       ```tsx
       {data.context.feedbackContext && (
         <div>
           <h4 className="text-xs font-medium text-zinc-400 uppercase tracking-wide mb-1">
             Feedback from Previous Cycle
           </h4>
           <pre className="max-h-48 overflow-y-auto bg-blue-50 border border-blue-100 p-3 rounded-md text-xs font-mono whitespace-pre-wrap break-words text-blue-900">
             {data.context.feedbackContext}
           </pre>
         </div>
       )}
       ```
    2. Use blue tones (bg-blue-50, text-blue-900, border-blue-100) to visually distinguish feedback context from other sections.

    **Part E: Update `src/app/(dashboard)/clients/[id]/page.tsx`**

    1. Add `cycle_number` to the client select query: change `.select('id, name, company, briefing, current_phase_number, status, created_at, updated_at')` to include `cycle_number`.
    2. Import `CycleBadge` from `@/components/clients/cycle-badge`.
    3. Import `ResetPipelineDialog` from `@/components/clients/reset-pipeline-dialog`.
    4. In the header section, add `<CycleBadge cycleNumber={client.cycle_number as number} />` after the Active/Archived badge.
    5. Determine `canReset`: check if there's a Phase 5 with status 'completed' in the phases data. `const phase5Completed = (phases ?? []).some((p: PhaseRow) => p.phase_number === 5 && p.status === 'completed')`.
    6. Add `<ResetPipelineDialog clientId={client.id} clientName={client.name} canReset={phase5Completed} />` in the button group next to Edit and ArchiveDialog.
  </action>
  <verify>
    <automated>cd /Users/gabrielviniciusoliveira/Agencia\ automatica && npx tsc --noEmit && grep -q "resetPipelineAction" src/lib/actions/pipeline-reset.ts && grep -q "CycleBadge" src/components/clients/cycle-badge.tsx && grep -q "feedbackContext" src/components/squad/PromptPreviewModal.tsx && grep -q "cycle_number" src/app/\(dashboard\)/clients/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "resetPipelineAction" src/lib/actions/pipeline-reset.ts
    - grep -q "reset_pipeline_cycle" src/lib/actions/pipeline-reset.ts
    - grep -q "revalidatePath" src/lib/actions/pipeline-reset.ts
    - grep -q "CycleBadge" src/components/clients/cycle-badge.tsx
    - grep -q "cycleNumber" src/components/clients/cycle-badge.tsx
    - grep -q "ResetPipelineDialog" src/components/clients/reset-pipeline-dialog.tsx
    - grep -q "feedbackContext" src/components/squad/PromptPreviewModal.tsx
    - grep -q "Feedback from Previous Cycle" src/components/squad/PromptPreviewModal.tsx
    - grep -q "cycle_number" src/app/(dashboard)/clients/[id]/page.tsx
    - grep -q "CycleBadge" src/app/(dashboard)/clients/[id]/page.tsx
    - grep -q "ResetPipelineDialog" src/app/(dashboard)/clients/[id]/page.tsx
  </acceptance_criteria>
  <done>
    resetPipelineAction calls reset_pipeline_cycle RPC with auth check. CycleBadge renders for cycle 2+. ResetPipelineDialog has confirmation flow with canReset guard. PromptPreviewModal shows feedback section when feedbackContext is non-empty. Client profile displays CycleBadge and ResetPipelineDialog. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration to Supabase and verify build</name>
  <files>supabase/migrations/00009_feedback_loop.sql</files>
  <read_first>
    - supabase/migrations/00009_feedback_loop.sql (verify migration content from Plan 01)
  </read_first>
  <action>
    1. Run `supabase db push` to apply migration 00009_feedback_loop.sql to the Supabase database.
       If the command prompts for confirmation, accept it.
       If it fails due to the UNIQUE constraint on phases(client_id, phase_number) conflicting with the reset approach,
       the migration may need adjustment -- the reset approach (updating existing rows in-place) should NOT conflict
       with the UNIQUE constraint since we are updating, not inserting duplicate rows.

    2. Run `npm run build` to verify the full Next.js production build passes with all changes.

    3. Run `npx vitest run` to verify all existing tests plus new feedback tests pass.

    4. If `supabase db push` fails, diagnose the error:
       - If it is a permissions issue, this becomes a checkpoint for the operator.
       - If it is a SQL syntax error, fix the migration and retry.
  </action>
  <verify>
    <automated>cd /Users/gabrielviniciusoliveira/Agencia\ automatica && npx vitest run && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>
    Migration applied to Supabase. Production build passes. All tests pass. The feedback loop is fully operational.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify feedback loop end-to-end</name>
  <files>n/a</files>
  <action>
    Present the verification steps to the operator for manual confirmation of the complete feedback loop system.
  </action>
  <what-built>
    Complete feedback loop system:
    1. Feedback context extraction from Phase 5 outputs (NPS, CLV, churn)
    2. Pipeline reset button on client profile for starting new cycles
    3. Cycle badge showing iteration number on client profile
    4. Feedback section in prompt preview modal
    5. Database migration with reset_pipeline_cycle RPC
  </what-built>
  <how-to-verify>
    1. Open any client profile page in the browser
    2. Verify the "Reset Pipeline (New Cycle)" button appears disabled (client has not completed Phase 5)
    3. If you have a test client that completed Phase 5: click the reset button, confirm, and verify:
       - Client returns to Phase 1 (Diagnostico) with all processes pending
       - A "Cycle 2" badge appears next to the client name
       - Previous squad job outputs are still accessible in the outputs page
    4. For a cycle 2+ client, trigger "Run Squad" on any Phase 1 process:
       - In the prompt preview modal, verify a blue "Feedback from Previous Cycle" section appears
       - The section should show NPS/CLV/churn data (or raw output if structured data was not available)
    5. Verify the app loads without console errors
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: manual verification required"</automated>
  </verify>
  <done>Operator confirms feedback loop works end-to-end.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI button -> Server Action | Operator triggers pipeline reset |
| Server Action -> Database RPC | Modifies multiple tables atomically |
| Prompt Preview -> feedbackContext | Displays previous cycle data in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Spoofing | resetPipelineAction | mitigate | Auth check at Server Action entry point -- unauthorized users cannot trigger reset |
| T-09-06 | Tampering | resetPipelineAction | mitigate | Zod validation on clientId (must be UUID); RPC guards check Phase 5 completion before allowing reset |
| T-09-07 | Repudiation | Pipeline reset | accept | Reset action is logged implicitly via updated_at timestamps on client and phase rows; explicit audit log deferred to v2 |
| T-09-08 | Information Disclosure | PromptPreviewModal | mitigate | feedbackContext is rendered as text node (no dangerouslySetInnerHTML); all data scoped to authenticated operator's view |
</threat_model>

<verification>
1. `npx tsc --noEmit` -- TypeScript compiles cleanly
2. `npx vitest run` -- all tests pass (including feedback.test.ts from Plan 01)
3. `npm run build` -- production build succeeds
4. Visual verification: client profile shows cycle badge, reset button, and prompt preview shows feedback context
</verification>

<success_criteria>
- Operator can reset a completed client's pipeline and see cycle_number increment
- CycleBadge appears for cycle 2+ clients
- Prompt preview modal shows feedback context with NPS/CLV/churn data
- Reset is guarded (only works after Phase 5 completion)
- Production build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-feedback-loop/09-02-SUMMARY.md`
</output>
