---
phase: 10-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - tests/db/connection.test.ts
  - tests/db/schema.test.ts
  - tests/db/triggers.test.ts
  - tests/db/pipeline.test.ts
  - tests/db/squad-jobs.test.ts
  - tests/db/helpers.ts
  - tests/setup.ts
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "All integration tests in tests/db/ execute against the live Supabase instance and pass"
    - "Test setup connects to the cloud Supabase project using credentials from .env.local"
    - "Test data is cleaned up after each test suite runs"
  artifacts:
    - path: "tests/db/connection.test.ts"
      provides: "Database connectivity and table existence verification"
    - path: "tests/db/schema.test.ts"
      provides: "Schema constraint validation (enums, required fields, foreign keys)"
    - path: "tests/db/triggers.test.ts"
      provides: "Trigger behavior tests (phase enforcement, auto-creation)"
    - path: "tests/db/pipeline.test.ts"
      provides: "Pipeline progression integration tests"
    - path: "tests/db/squad-jobs.test.ts"
      provides: "Squad job lifecycle tests"
  key_links:
    - from: "tests/setup.ts"
      to: ".env.local"
      via: "dotenv loading NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE_URL"
    - from: "tests/db/*.test.ts"
      to: "tests/setup.ts"
      via: "testClient import"
      pattern: "import.*testClient.*from.*setup"
---

<objective>
Execute all integration tests against the live Supabase instance and fix any failures.

Purpose: The integration tests in tests/db/ were written during v1.0 development but never executed against the live database. They test critical behaviors: schema constraints, trigger enforcement, pipeline progression, and job lifecycle. Running them validates that the production database schema matches what the application code expects.

Output: All 5 integration test suites pass against the live Supabase instance, any schema mismatches or test bugs fixed
</objective>

<execution_context>
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-CONTEXT.md
@.planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md

Key files:
@tests/setup.ts
@tests/db/helpers.ts
@tests/db/connection.test.ts
@tests/db/schema.test.ts
@tests/db/triggers.test.ts
@tests/db/pipeline.test.ts
@tests/db/squad-jobs.test.ts
@vitest.config.ts

<interfaces>
<!-- Test infrastructure contracts -->

From tests/setup.ts:
```typescript
export const testClient: SupabaseClient<Database>  // service-role client, bypasses RLS
export async function cleanTestData(): Promise<void>  // deletes all test data in FK order
```

From tests/db/helpers.ts:
```typescript
export async function createTestClientWithProcesses(): Promise<{
  clientId: string
  phases: Phase[]
  processes: Process[]
  gates: Gate[]
}>
```

The tests use the cloud Supabase URL and service role key from .env.local.
The vitest config includes all tests/\*\*/\*.test.ts with 30s timeout and no file parallelism.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute integration tests and fix failures</name>
  <files>tests/db/connection.test.ts, tests/db/schema.test.ts, tests/db/triggers.test.ts, tests/db/pipeline.test.ts, tests/db/squad-jobs.test.ts, tests/db/helpers.ts, tests/setup.ts</files>
  <action>
1. First, verify Supabase credentials are available:
   ```bash
   grep -q "NEXT_PUBLIC_SUPABASE_URL" .env.local && grep -q "SUPABASE_SERVICE_ROLE_KEY" .env.local && echo "Credentials found"
   ```
   If credentials are missing, this is a blocker -- create a checkpoint for the user.

2. Run the full integration test suite:
   ```bash
   npm test -- tests/db/
   ```

3. Analyze failures. Common expected failure categories:

   a. **Missing gate_reviews table in test expectations**: connection.test.ts checks for 6 core tables but gate_reviews (added in migration 00008) is the 7th. Update EXPECTED_TABLES to include 'gate_reviews' if the test fails on table count.

   b. **`(testClient as any)` cast in connection.test.ts line 36**: After Plan 01 regenerated types, the gate_reviews table and all other tables are in the Database type. The `(testClient as any).from(tableName)` cast was needed because the hand-generated types only had 6 tables. Now with full types, the dynamic table name iteration still needs the cast (TypeScript can't narrow a runtime string to a table literal). Leave this specific test cast if it's the only way to iterate dynamically -- it's test infrastructure, not application code.

   c. **Schema test failures**: schema.test.ts tests CHECK constraints. Verify the constraint names and values match the live database. The squad_type constraint was extended in migration 00008 to include 'gate_review' -- if tests check the original 4-value constraint, update them.

   d. **Trigger test failures**: triggers.test.ts tests auto-creation of phases/processes/gates. May fail if the live schema has different trigger behavior than expected (e.g., number of processes created, feedback loop fields).

   e. **Pipeline test failures**: pipeline.test.ts tests gate approval/rejection progression. May fail if RPC function signatures changed or if test data setup doesn't match current schema requirements.

   f. **Squad jobs test failures**: squad-jobs.test.ts tests job lifecycle. May fail if the squad_type constraint now requires 'gate_review' support or if new columns (structured_output) aren't accounted for.

4. For each failure, fix the test to match the actual live schema behavior. Do NOT change the production schema or application code to match outdated tests. Tests must adapt to reality.

5. After fixing, run the full suite again:
   ```bash
   npm test -- tests/db/
   ```
   Repeat until all tests pass.

6. Also run the unit tests to verify no regressions from Plan 01 type changes:
   ```bash
   npm test
   ```
   All tests (unit + integration) must pass.

7. Run a final TypeScript check to confirm test file changes compile:
   ```bash
   npx tsc --noEmit
   ```
  </action>
  <verify>
    <automated>cd "/Users/gabrielviniciusoliveira/Agencia automatica" && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All integration tests in tests/db/ pass against the live Supabase instance
    - All unit tests continue to pass (no regressions from type changes)
    - Test expectations match the actual live database schema (7 tables including gate_reviews, extended squad_type constraint, all RPCs)
    - npx tsc --noEmit passes with zero errors
    - Test data cleanup works correctly (cleanTestData deletes in FK order)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite -> live database | Tests run with service-role key against production Supabase; must clean up all test data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | I (Information Disclosure) | tests/setup.ts | mitigate | Service role key loaded from .env.local (gitignored). Fallback local key is the well-known Supabase demo key (not a secret). |
| T-10-04 | T (Tampering) | cleanTestData | mitigate | cleanTestData uses sentinel UUID neq pattern to delete all rows. Tests must always call cleanTestData in afterAll to avoid polluting production data. Verify afterAll hooks exist in every test file. |
</threat_model>

<verification>
1. `npm test -- tests/db/connection.test.ts` passes
2. `npm test -- tests/db/schema.test.ts` passes
3. `npm test -- tests/db/triggers.test.ts` passes
4. `npm test -- tests/db/pipeline.test.ts` passes
5. `npm test -- tests/db/squad-jobs.test.ts` passes
6. `npm test` (full suite) passes with zero failures
7. `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
- All 5 integration test suites pass against the live Supabase instance
- All unit tests pass (no regressions)
- TypeScript compiles clean
- No test data left in the live database after test runs
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-02-SUMMARY.md`
</output>
