---
phase: 19-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/loading.tsx
  - src/app/(dashboard)/clients/loading.tsx
  - src/app/(dashboard)/clients/[id]/loading.tsx
  - src/app/(dashboard)/analytics/loading.tsx
  - src/app/(dashboard)/costs/loading.tsx
  - src/app/(dashboard)/templates/loading.tsx
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "Every dashboard route has a loading.tsx file that renders skeleton placeholders during data fetch"
    - "Skeleton blocks use animate-pulse with bg-[#E8EDED] (gray-100) on white backgrounds"
    - "Each skeleton matches the shape of the actual route content (Kanban columns, card grids, tables, etc.)"
    - "Skeleton containers have role='status' and aria-label='Carregando...' for accessibility"
  artifacts:
    - src/app/(dashboard)/loading.tsx
    - src/app/(dashboard)/clients/loading.tsx
    - src/app/(dashboard)/clients/[id]/loading.tsx
    - src/app/(dashboard)/analytics/loading.tsx
    - src/app/(dashboard)/costs/loading.tsx
    - src/app/(dashboard)/templates/loading.tsx
  key_links:
    - "Next.js App Router loading.tsx convention auto-wraps in Suspense — no manual Suspense needed"
    - "Skeleton shapes derived from actual page layouts in corresponding page.tsx files"
---

<objective>
Create skeleton loading screens for all 6 dashboard routes so the operator sees content-shaped placeholders while data loads instead of blank pages or spinners.

Purpose: Addresses UX-01 (skeleton screens). Each route gets a `loading.tsx` file using the Next.js App Router convention for automatic Suspense streaming. Skeletons match the actual content shape per D-01, D-02, D-03 from CONTEXT.md.

Output: 6 new `loading.tsx` files, one per dashboard route segment.
</objective>

<execution_context>
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ux-polish/19-CONTEXT.md
@.planning/phases/19-ux-polish/19-RESEARCH.md
@.planning/phases/19-ux-polish/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton loading screens for Dashboard and Clients routes</name>
  <files>src/app/(dashboard)/loading.tsx, src/app/(dashboard)/clients/loading.tsx, src/app/(dashboard)/clients/[id]/loading.tsx</files>
  <read_first>
    - src/app/(dashboard)/page.tsx (Dashboard layout — h1 + BottleneckAlert + CostSummaryWidget + KanbanBoard with 5 columns)
    - src/app/(dashboard)/clients/page.tsx (Client list — renders ClientGrid)
    - src/components/clients/client-grid.tsx (Grid layout — cards in responsive grid)
    - src/app/(dashboard)/clients/[id]/page.tsx (Client profile — header + briefing + pipeline + outputs)
    - .planning/phases/19-ux-polish/19-UI-SPEC.md (Skeleton Component Inventory section)
  </read_first>
  <action>
Create 3 `loading.tsx` files. Each exports a default function returning skeleton placeholders. All skeletons use `animate-pulse` with `bg-[#E8EDED]` blocks on white card backgrounds (`bg-white rounded-md`). Per D-01, D-02, D-03.

**1. `src/app/(dashboard)/loading.tsx` — Dashboard (Kanban) skeleton:**
```tsx
export default function DashboardLoading() {
  return (
    <div role="status" aria-label="Carregando..." aria-busy="true" className="space-y-6">
      {/* Page title */}
      <div className="h-8 w-40 bg-[#E8EDED] rounded-md animate-pulse" />
      {/* Alert placeholder */}
      <div className="h-12 w-full bg-[#E8EDED] rounded-md animate-pulse" />
      {/* Cost widget placeholder */}
      <div className="h-16 w-full bg-white rounded-md animate-pulse border border-[#E8EDED]" />
      {/* Kanban: 5 columns */}
      <div className="grid grid-cols-5 gap-4">
        {Array.from({ length: 5 }).map((_, col) => (
          <div key={col} className="space-y-3">
            <div className="h-6 w-24 bg-[#E8EDED] rounded-md animate-pulse" />
            {Array.from({ length: 3 }).map((_, card) => (
              <div key={card} className="h-20 w-full bg-white rounded-md animate-pulse border border-[#E8EDED]" />
            ))}
          </div>
        ))}
      </div>
    </div>
  )
}
```

**2. `src/app/(dashboard)/clients/loading.tsx` — Client grid skeleton:**
```tsx
export default function ClientsLoading() {
  return (
    <div role="status" aria-label="Carregando..." aria-busy="true" className="space-y-6">
      {/* Header area */}
      <div className="flex items-center justify-between">
        <div className="h-8 w-32 bg-[#E8EDED] rounded-md animate-pulse" />
        <div className="h-10 w-28 bg-[#E8EDED] rounded-md animate-pulse" />
      </div>
      {/* Card grid: 6 cards in 3x2 */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {Array.from({ length: 6 }).map((_, i) => (
          <div key={i} className="h-32 w-full bg-white rounded-md animate-pulse border border-[#E8EDED]" />
        ))}
      </div>
    </div>
  )
}
```

**3. `src/app/(dashboard)/clients/[id]/loading.tsx` — Client profile skeleton:**
```tsx
export default function ClientProfileLoading() {
  return (
    <div role="status" aria-label="Carregando..." aria-busy="true" className="max-w-3xl space-y-8">
      {/* Header */}
      <div className="flex items-start justify-between gap-4">
        <div className="space-y-2">
          <div className="h-8 w-48 bg-[#E8EDED] rounded-md animate-pulse" />
          <div className="h-4 w-32 bg-[#E8EDED] rounded-md animate-pulse" />
        </div>
        <div className="flex gap-2">
          <div className="h-9 w-16 bg-[#E8EDED] rounded-md animate-pulse" />
          <div className="h-9 w-16 bg-[#E8EDED] rounded-md animate-pulse" />
        </div>
      </div>
      {/* Tab bar */}
      <div className="flex gap-2">
        {Array.from({ length: 3 }).map((_, i) => (
          <div key={i} className="h-10 w-24 bg-[#E8EDED] rounded-md animate-pulse" />
        ))}
      </div>
      {/* Content area */}
      <div className="h-64 w-full bg-white rounded-md animate-pulse border border-[#E8EDED]" />
    </div>
  )
}
```
  </action>
  <acceptance_criteria>
    - `src/app/(dashboard)/loading.tsx` exists and exports a default function
    - `src/app/(dashboard)/loading.tsx` contains `role="status"` and `aria-label="Carregando..."`
    - `src/app/(dashboard)/loading.tsx` contains `animate-pulse` and `bg-[#E8EDED]`
    - `src/app/(dashboard)/loading.tsx` contains `grid-cols-5` (5 Kanban columns)
    - `src/app/(dashboard)/clients/loading.tsx` exists and contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
    - `src/app/(dashboard)/clients/[id]/loading.tsx` exists and contains `max-w-3xl`
    - All 3 files contain `aria-busy="true"`
  </acceptance_criteria>
  <verify>
    <automated>test -f "src/app/(dashboard)/loading.tsx" && test -f "src/app/(dashboard)/clients/loading.tsx" && test -f "src/app/(dashboard)/clients/[id]/loading.tsx" && grep -q "animate-pulse" "src/app/(dashboard)/loading.tsx" && grep -q "Carregando" "src/app/(dashboard)/clients/loading.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Dashboard, clients list, and client profile routes each have a loading.tsx with content-shaped skeleton placeholders using animate-pulse on gray-100 blocks</done>
</task>

<task type="auto">
  <name>Task 2: Create skeleton loading screens for Analytics, Costs, and Templates routes</name>
  <files>src/app/(dashboard)/analytics/loading.tsx, src/app/(dashboard)/costs/loading.tsx, src/app/(dashboard)/templates/loading.tsx</files>
  <read_first>
    - src/app/(dashboard)/analytics/page.tsx (Analytics — h1 + description + AnalyticsDashboard)
    - src/app/(dashboard)/costs/page.tsx (Costs — h1 + MonthSelector + cost total + CostBreakdownTable)
    - src/app/(dashboard)/templates/page.tsx (Templates — h1 + description + TemplateList)
    - .planning/phases/19-ux-polish/19-UI-SPEC.md (Skeleton Component Inventory section)
  </read_first>
  <action>
Create 3 `loading.tsx` files for the remaining routes. Same conventions: `animate-pulse`, `bg-[#E8EDED]`, `role="status"`, `aria-label="Carregando..."`. Per D-01, D-02, D-03.

**1. `src/app/(dashboard)/analytics/loading.tsx` — Analytics skeleton:**
```tsx
export default function AnalyticsLoading() {
  return (
    <div role="status" aria-label="Carregando..." aria-busy="true" className="max-w-6xl space-y-6">
      {/* Header */}
      <div className="space-y-1">
        <div className="h-8 w-32 bg-[#E8EDED] rounded-md animate-pulse" />
        <div className="h-4 w-64 bg-[#E8EDED] rounded-md animate-pulse" />
      </div>
      {/* Stat cards row */}
      <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <div key={i} className="h-20 bg-white rounded-md animate-pulse border border-[#E8EDED]" />
        ))}
      </div>
      {/* Chart placeholders */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <div className="h-48 bg-white rounded-md animate-pulse border border-[#E8EDED]" />
        <div className="h-48 bg-white rounded-md animate-pulse border border-[#E8EDED]" />
      </div>
    </div>
  )
}
```

**2. `src/app/(dashboard)/costs/loading.tsx` — Costs table skeleton:**
```tsx
export default function CostsLoading() {
  return (
    <div role="status" aria-label="Carregando..." aria-busy="true" className="max-w-4xl space-y-6">
      {/* Header with month selector */}
      <div className="flex items-center justify-between">
        <div className="h-8 w-40 bg-[#E8EDED] rounded-md animate-pulse" />
        <div className="h-10 w-32 bg-[#E8EDED] rounded-md animate-pulse" />
      </div>
      {/* Total amount */}
      <div className="space-y-1">
        <div className="h-4 w-24 bg-[#E8EDED] rounded-md animate-pulse" />
        <div className="h-7 w-20 bg-[#E8EDED] rounded-md animate-pulse" />
      </div>
      {/* Table skeleton */}
      <div className="space-y-0">
        <div className="h-10 w-full bg-[#E8EDED] rounded-t-md animate-pulse" />
        {Array.from({ length: 8 }).map((_, i) => (
          <div key={i} className="h-12 w-full bg-white animate-pulse border-b border-[#E8EDED]" />
        ))}
      </div>
    </div>
  )
}
```

**3. `src/app/(dashboard)/templates/loading.tsx` — Templates list skeleton:**
```tsx
export default function TemplatesLoading() {
  return (
    <div role="status" aria-label="Carregando..." aria-busy="true" className="max-w-3xl space-y-6">
      {/* Header */}
      <div className="space-y-1">
        <div className="h-8 w-32 bg-[#E8EDED] rounded-md animate-pulse" />
        <div className="h-4 w-80 bg-[#E8EDED] rounded-md animate-pulse" />
      </div>
      {/* List items */}
      {Array.from({ length: 4 }).map((_, i) => (
        <div key={i} className="h-16 w-full bg-white rounded-md animate-pulse border border-[#E8EDED]" />
      ))}
    </div>
  )
}
```
  </action>
  <acceptance_criteria>
    - `src/app/(dashboard)/analytics/loading.tsx` exists and contains `grid-cols-2 lg:grid-cols-4` (4 stat cards)
    - `src/app/(dashboard)/analytics/loading.tsx` contains `h-48` (chart placeholders)
    - `src/app/(dashboard)/costs/loading.tsx` exists and contains 8 table row skeletons
    - `src/app/(dashboard)/costs/loading.tsx` contains `max-w-4xl` matching the costs page
    - `src/app/(dashboard)/templates/loading.tsx` exists and contains `max-w-3xl` matching templates page
    - All 3 files contain `role="status"` and `aria-label="Carregando..."` and `aria-busy="true"`
    - All 3 files contain `animate-pulse` and `bg-[#E8EDED]`
  </acceptance_criteria>
  <verify>
    <automated>test -f "src/app/(dashboard)/analytics/loading.tsx" && test -f "src/app/(dashboard)/costs/loading.tsx" && test -f "src/app/(dashboard)/templates/loading.tsx" && grep -q "Carregando" "src/app/(dashboard)/analytics/loading.tsx" && grep -q "animate-pulse" "src/app/(dashboard)/costs/loading.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Analytics, costs, and templates routes each have a loading.tsx with content-shaped skeleton placeholders</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates static loading UI components — no data flow, no user input, no API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | N/A | loading.tsx files | accept | Skeleton screens are pure presentational React components with no dynamic content or user input |
</threat_model>

<verification>
1. `npx next build` completes without errors (all 6 loading.tsx files compile)
2. Each route segment has a loading.tsx file: dashboard, clients, clients/[id], analytics, costs, templates
3. All loading.tsx files use animate-pulse and accessible aria attributes
4. Skeleton shapes match actual content layout (5 Kanban columns, 3x2 card grid, table rows, etc.)
</verification>

<success_criteria>
- All 6 dashboard routes display skeleton placeholders during data fetching
- Skeletons use consistent styling (animate-pulse, gray-100 on white)
- Skeletons match actual content shapes (verifiable by comparing loading.tsx layout to page.tsx layout)
- Accessibility attributes present on all skeleton containers
- Build succeeds with no TypeScript or compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-ux-polish/19-01-SUMMARY.md`
</output>
