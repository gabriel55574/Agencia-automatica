---
phase: 19-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/empty-state.tsx
  - src/app/(dashboard)/page.tsx
  - src/components/clients/client-grid.tsx
  - src/app/(dashboard)/templates/page.tsx
  - src/app/(dashboard)/costs/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
  - src/components/dashboard/KanbanColumn.tsx
autonomous: true
requirements: [VIS-03]
must_haves:
  truths:
    - "A reusable EmptyState component exists at src/components/ui/empty-state.tsx"
    - "EmptyState accepts icon, title, description, actionLabel, and actionHref props"
    - "EmptyState renders a Lucide icon at 48px in gray-300, title in heading-sm, description in body-md gray-400, and optional CTA button"
    - "All 6 empty state locations display the EmptyState component with appropriate icon, PT-BR copy, and CTA"
    - "Empty state CTA buttons use the default Button variant (Electric Lime background per brand identity)"
  artifacts:
    - src/components/ui/empty-state.tsx
  key_links:
    - "EmptyState component consumed by dashboard, client-grid, templates, costs, analytics pages and KanbanColumn"
    - "Copy and icon choices from CONTEXT.md D-04, D-05, D-06 and UI-SPEC copywriting contract"
---

<objective>
Create a reusable EmptyState component and integrate it into all pages that need empty data states, replacing bare text with icon + descriptive PT-BR copy + CTA buttons.

Purpose: Addresses VIS-03 (empty states). Implements D-04 (pattern), D-05 (reusable component), D-06 (6 empty state locations) from CONTEXT.md. All copy in PT-BR per Phase 18 requirement.

Output: New EmptyState component + updates to 6 page/component files.
</objective>

<execution_context>
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ux-polish/19-CONTEXT.md
@.planning/phases/19-ux-polish/19-RESEARCH.md
@.planning/phases/19-ux-polish/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable EmptyState component</name>
  <files>src/components/ui/empty-state.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (Button component — verify variant="default" maps to primary/Electric Lime)
    - .planning/phases/19-ux-polish/19-UI-SPEC.md (EmptyState Component section — props, layout, styling)
    - .planning/phases/19-ux-polish/19-CONTEXT.md (D-04, D-05 — pattern and props)
  </read_first>
  <action>
Create `src/components/ui/empty-state.tsx` — a reusable component per D-05.

```tsx
import Link from 'next/link'
import type { LucideIcon } from 'lucide-react'
import { Button } from '@/components/ui/button'

interface EmptyStateProps {
  icon: LucideIcon
  title: string
  description: string
  actionLabel?: string
  actionHref?: string
  onAction?: () => void
}

export function EmptyState({
  icon: Icon,
  title,
  description,
  actionLabel,
  actionHref,
  onAction,
}: EmptyStateProps) {
  return (
    <div className="flex flex-col items-center justify-center py-16 px-4">
      <div className="flex flex-col items-center gap-4 max-w-[320px] text-center">
        <Icon className="h-12 w-12 text-[#8A9999]" aria-hidden="true" />
        <h3 className="text-xl font-semibold font-heading text-foreground">
          {title}
        </h3>
        <p className="text-base text-[#5C6E6E]">
          {description}
        </p>
        {actionLabel && actionHref && (
          <Link href={actionHref}>
            <Button>{actionLabel}</Button>
          </Link>
        )}
        {actionLabel && onAction && !actionHref && (
          <Button onClick={onAction}>{actionLabel}</Button>
        )}
      </div>
    </div>
  )
}
```

Per D-04: icon 48px (h-12 w-12), gray-300 color (#8A9999), title in heading-sm (text-xl font-semibold font-heading = 20px Montserrat 600), description in body-md (text-base = 16px) gray-400 (#5C6E6E). CTA uses default Button (Electric Lime per brand). Icon is `aria-hidden="true"` per UI-SPEC accessibility notes.

The component accepts either `actionHref` for Link-based navigation or `onAction` for callback-based actions (needed for tab switching in outputs empty state).
  </action>
  <acceptance_criteria>
    - `src/components/ui/empty-state.tsx` exists
    - File contains `export function EmptyState`
    - File contains `interface EmptyStateProps` with `icon: LucideIcon`, `title: string`, `description: string`, `actionLabel?: string`, `actionHref?: string`
    - File contains `h-12 w-12` (48px icon)
    - File contains `text-[#8A9999]` (gray-300 icon color)
    - File contains `text-[#5C6E6E]` (gray-400 description color)
    - File contains `aria-hidden="true"` on the icon
    - File contains `max-w-[320px]`
    - File imports `Button` from `@/components/ui/button`
    - File imports `Link` from `next/link`
  </acceptance_criteria>
  <verify>
    <automated>test -f "src/components/ui/empty-state.tsx" && grep -q "EmptyState" "src/components/ui/empty-state.tsx" && grep -q "LucideIcon" "src/components/ui/empty-state.tsx" && grep -q "aria-hidden" "src/components/ui/empty-state.tsx" && grep -q "8A9999" "src/components/ui/empty-state.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>EmptyState component created with icon, title, description, and optional CTA. Uses brand colors and accessibility attributes per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate EmptyState into Dashboard, Clients, Templates, Costs, and Analytics</name>
  <files>src/components/dashboard/KanbanColumn.tsx, src/components/clients/client-grid.tsx, src/app/(dashboard)/templates/page.tsx, src/app/(dashboard)/costs/page.tsx, src/app/(dashboard)/analytics/page.tsx, src/app/(dashboard)/page.tsx</files>
  <read_first>
    - src/components/dashboard/KanbanColumn.tsx (line 25: current "No clients" text)
    - src/components/clients/client-grid.tsx (line 55: current "No clients yet." text)
    - src/app/(dashboard)/templates/page.tsx (lines 34-39: current "No templates yet" text)
    - src/app/(dashboard)/costs/page.tsx (current layout — no empty state exists)
    - src/app/(dashboard)/analytics/page.tsx (current layout — no empty state exists)
    - src/app/(dashboard)/page.tsx (Dashboard page — KanbanBoard rendered, need top-level empty state)
    - src/components/costs/CostBreakdownTable.tsx (check if it handles empty rows)
    - .planning/phases/19-ux-polish/19-CONTEXT.md (D-06 — all 6 empty state locations and their copy)
    - .planning/phases/19-ux-polish/19-UI-SPEC.md (Copywriting Contract — exact PT-BR text for each)
  </read_first>
  <action>
Integrate EmptyState component into 6 locations per D-06. All copy from UI-SPEC Copywriting Contract.

**1. KanbanColumn.tsx** — Replace the plain "No clients" text in the empty column state:
Replace: `<p className="text-xs text-zinc-400 text-center py-8">No clients</p>`
With: Keep simple for per-column empty (not the full EmptyState — columns are small). Change text to PT-BR:
```tsx
<p className="text-xs text-[#8A9999] text-center py-8">Nenhum cliente</p>
```

**2. Dashboard page.tsx** — Add empty state when there are zero clients total. After fetching `data` from `fetchDashboardData`, check if all columns are empty. If `data.clients` (or equivalent data property) has zero length, render EmptyState instead of KanbanBoard:
```tsx
import { Users } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'

// Inside the return, wrap KanbanBoard with a check:
{data.activeClients.length === 0 && !showArchived ? (
  <EmptyState
    icon={Users}
    title="Nenhum cliente no pipeline"
    description="Seus clientes aparecerao aqui organizados por fase."
    actionLabel="Novo Cliente"
    actionHref="/clients/new"
  />
) : (
  <KanbanBoard data={data} showArchived={showArchived} />
)}
```
Read the `fetchDashboardData` return type to identify the correct property name for checking empty clients (likely `data.activeClients` or `data.clients`). Adjust the condition accordingly.

**3. client-grid.tsx** — Replace the current empty state markup (approximately line 50-58) with EmptyState:
```tsx
import { Users } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'

// Replace the existing "No clients yet." block:
<EmptyState
  icon={Users}
  title="Nenhum cliente cadastrado"
  description="Adicione seu primeiro cliente para comecar a gerenciar o pipeline."
  actionLabel="Novo Cliente"
  actionHref="/clients/new"
/>
```

**4. templates/page.tsx** — Replace the existing "No templates yet" block (lines 34-39):
```tsx
import { FileText } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'

// Replace:
<EmptyState
  icon={FileText}
  title="Nenhum template salvo"
  description="Templates de prompt facilitam a execucao das squads."
  actionLabel="Criar Template"
  actionHref="/templates"
/>
```
Note: The "Criar Template" CTA is contextual — templates are saved from squad outputs, not created directly. The href should link to the most relevant page. If there is no direct "create template" route, omit the CTA and make it informational only, or use the templates page itself.

**5. costs/page.tsx** — Add empty state when `rows` is empty:
```tsx
import { DollarSign } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'

// After the total display, before CostBreakdownTable:
{rows.length === 0 ? (
  <EmptyState
    icon={DollarSign}
    title="Nenhum dado de custo"
    description="Custos de execucao das squads serao registrados automaticamente."
  />
) : (
  <CostBreakdownTable rows={rows} />
)}
```

**6. analytics/page.tsx** — Add empty state check. Read `fetchAnalyticsData` return type to determine the right empty condition. If data indicates no analytics available:
```tsx
import { BarChart3 } from 'lucide-react'
import { EmptyState } from '@/components/ui/empty-state'

// Check if analytics data is empty (read the data shape to determine the right condition):
{/* If data is empty/insufficient: */}
<EmptyState
  icon={BarChart3}
  title="Dados insuficientes"
  description="Analises estarao disponiveis apos a execucao de squads."
/>
```
Read `src/lib/analytics/queries.ts` to understand the data shape and determine the correct empty-check condition (e.g., `data.totalClients === 0` or `data.completedJobs === 0`).
  </action>
  <acceptance_criteria>
    - `src/components/dashboard/KanbanColumn.tsx` contains "Nenhum cliente" (PT-BR)
    - `src/app/(dashboard)/page.tsx` imports `EmptyState` and `Users` from lucide-react
    - `src/app/(dashboard)/page.tsx` contains "Nenhum cliente no pipeline"
    - `src/components/clients/client-grid.tsx` imports `EmptyState` and `Users`
    - `src/components/clients/client-grid.tsx` contains "Nenhum cliente cadastrado"
    - `src/app/(dashboard)/templates/page.tsx` contains "Nenhum template salvo"
    - `src/app/(dashboard)/costs/page.tsx` imports `EmptyState` and `DollarSign`
    - `src/app/(dashboard)/costs/page.tsx` contains "Nenhum dado de custo"
    - `src/app/(dashboard)/analytics/page.tsx` imports `EmptyState` and `BarChart3`
    - `src/app/(dashboard)/analytics/page.tsx` contains "Dados insuficientes"
    - No file contains "No clients yet." or "No templates yet" (old English text replaced)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Nenhum cliente no pipeline" "src/app/(dashboard)/page.tsx" && grep -q "Nenhum cliente cadastrado" "src/components/clients/client-grid.tsx" && grep -q "Nenhum template salvo" "src/app/(dashboard)/templates/page.tsx" && grep -q "Nenhum dado de custo" "src/app/(dashboard)/costs/page.tsx" && grep -q "Dados insuficientes" "src/app/(dashboard)/analytics/page.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 6 empty state locations use the EmptyState component with Lucide icons, PT-BR copy, and appropriate CTA buttons per CONTEXT.md D-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | EmptyState is a presentational component — no data mutations, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | N/A | EmptyState component | accept | Purely presentational — renders static text and links, no dynamic content from untrusted sources |
</threat_model>

<verification>
1. `npx next build` completes without errors
2. EmptyState component renders correctly in all 6 locations
3. Empty state copy matches PT-BR text from UI-SPEC copywriting contract
4. CTA buttons navigate to correct destinations
5. Icons render at 48px with gray-300 color
</verification>

<success_criteria>
- Reusable EmptyState component exists with icon, title, description, optional CTA
- All 6 empty state locations display the component with correct PT-BR copy
- CTA buttons use Electric Lime (default Button) and navigate correctly
- No old English empty state text remains ("No clients yet", "No templates yet", etc.)
- Build succeeds with no TypeScript or compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-ux-polish/19-02-SUMMARY.md`
</output>
