---
phase: 16-brand-identity-sidebar-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "All Velocity CSS custom properties from identity doc section 4 are available as Tailwind utilities"
    - "Montserrat and Roboto fonts load via next/font/google with zero layout shift"
    - "Tailwind utilities font-heading and font-body resolve to Montserrat and Roboto respectively"
    - "shadcn components inherit brand colors via mapped CSS variables"
  artifacts:
    - src/app/globals.css
    - src/app/layout.tsx
  key_links:
    - "globals.css @theme block creates bg-velocity-black, text-velocity-lime, etc. utility classes"
    - "layout.tsx CSS variables --font-montserrat and --font-roboto available on html element"
---

<objective>
Inject Velocity brand identity tokens into the design system and swap fonts from Geist to Montserrat + Roboto.

Purpose: Establish the visual foundation (colors, typography, spacing, shadows, radii) that all subsequent UI work in this phase and future phases builds upon. Addresses VIS-02 (brand identity).

Output: Updated globals.css with full Velocity token set, updated root layout with brand fonts.
</objective>

<execution_context>
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielviniciusoliveira/Agencia automatica/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/VELOCITY-UI-IDENTITY-SYSTEM.md
@.planning/phases/16-brand-identity-sidebar-layout/16-CONTEXT.md
@.planning/phases/16-brand-identity-sidebar-layout/16-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject Velocity CSS custom properties and Tailwind theme tokens into globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current state — minimal vars, @theme inline block with Geist refs)
    - docs/VELOCITY-UI-IDENTITY-SYSTEM.md (section 4: CSS Custom Properties, section 5: Light/Dark themes, section 11: Tailwind config reference)
  </read_first>
  <action>
Replace the entire contents of `src/app/globals.css` with the following structure:

1. **Import:** Keep `@import "tailwindcss";` at top.

2. **`:root` block** — Inject ALL CSS custom properties from identity doc section 4:
   - Primary colors: `--color-black: #062222`, `--color-lime: #BFF205`, `--color-white: #F2F2F2`
   - Derived colors: `--color-lime-hover: #ACD904`, `--color-lime-active: #93BA04`, `--color-lime-light: #F8FDE6`, `--color-lime-dark: #6D8A03`, `--color-black-soft: #0A3333`, `--color-black-deep: #041919`
   - Grays: `--color-gray-100: #E8EDED`, `--color-gray-200: #CBD4D4`, `--color-gray-300: #8A9999`, `--color-gray-400: #5C6E6E`
   - Semantic: `--color-success: #BFF205`, `--color-error: #EF4444`, `--color-warning: #F59E0B`, `--color-info: #3B82F6`
   - Spacing: `--space-1: 4px` through `--space-24: 96px` (all 12 tokens from section 4)
   - Border radius: `--radius-sm: 4px` through `--radius-full: 9999px` (6 tokens)
   - Shadows: `--shadow-sm` through `--shadow-xl` (4 tokens, using `rgba(6, 34, 34, ...)`)
   - Transitions: `--transition-fast: 150ms ease`, `--transition-normal: 250ms ease`, `--transition-slow: 400ms ease`

3. **`:root` light theme mapping** — Map shadcn expected variables to Velocity tokens for the content area (light theme per D-02):
   - `--background: #F2F2F2` (was #ffffff)
   - `--foreground: #062222` (was #171717)
   - `--primary: #BFF205`
   - `--primary-foreground: #062222`
   - `--destructive: #EF4444`
   - `--destructive-foreground: #FFFFFF`
   - `--muted: #E8EDED`
   - `--muted-foreground: #5C6E6E`
   - `--accent: #F8FDE6`
   - `--accent-foreground: #6D8A03`
   - `--border: #E8EDED`
   - `--input: #E8EDED`
   - `--ring: #BFF205`
   - `--card: #FFFFFF`
   - `--card-foreground: #062222`
   - `--popover: #FFFFFF`
   - `--popover-foreground: #062222`
   - `--secondary: #F2F2F2`
   - `--secondary-foreground: #062222`

4. **`@theme` block** — Define static Velocity color tokens as Tailwind utilities:
   ```css
   @theme {
     --color-velocity-black: #062222;
     --color-velocity-lime: #BFF205;
     --color-velocity-white: #F2F2F2;
     --color-velocity-lime-hover: #ACD904;
     --color-velocity-lime-active: #93BA04;
     --color-velocity-lime-light: #F8FDE6;
     --color-velocity-lime-dark: #6D8A03;
     --color-velocity-black-soft: #0A3333;
     --color-velocity-black-deep: #041919;
     --color-velocity-gray-100: #E8EDED;
     --color-velocity-gray-200: #CBD4D4;
     --color-velocity-gray-300: #8A9999;
     --color-velocity-gray-400: #5C6E6E;
     --color-velocity-error: #EF4444;
     --color-velocity-warning: #F59E0B;
     --color-velocity-info: #3B82F6;
   }
   ```

5. **`@theme inline` block** — Reference next/font CSS variables for Tailwind font utilities:
   ```css
   @theme inline {
     --font-heading: var(--font-montserrat);
     --font-body: var(--font-roboto);
     --color-background: var(--background);
     --color-foreground: var(--foreground);
   }
   ```

6. **Remove** the `@media (prefers-color-scheme: dark)` block entirely — per D-02, the app uses a hybrid theme (dark sidebar, light content), NOT system-preference-based dark mode. Dark mode is out of scope per REQUIREMENTS.md.

7. **`body` rule** — Update to use brand font:
   ```css
   body {
     background: var(--background);
     color: var(--foreground);
     font-family: var(--font-body), sans-serif;
   }
   ```

Per D-04: implement all CSS custom properties from section 4 of the identity doc as Tailwind theme extensions. Per D-05: Electric Lime (#BFF205) must NOT be used as text on light backgrounds — the `--accent-foreground` is set to Lime Dark (#6D8A03) for this reason.
  </action>
  <acceptance_criteria>
    - `src/app/globals.css` contains `--color-black: #062222`
    - `src/app/globals.css` contains `--color-lime: #BFF205`
    - `src/app/globals.css` contains `--color-velocity-black: #062222` inside `@theme {`
    - `src/app/globals.css` contains `--font-heading: var(--font-montserrat)` inside `@theme inline {`
    - `src/app/globals.css` contains `--background: #F2F2F2`
    - `src/app/globals.css` does NOT contain `prefers-color-scheme: dark`
    - `src/app/globals.css` does NOT contain `--font-geist-sans`
    - `src/app/globals.css` contains `--space-1: 4px`
    - `src/app/globals.css` contains `--radius-lg: 12px`
    - `src/app/globals.css` contains `--shadow-md: 0 4px 6px rgba(6, 34, 34, 0.08)`
    - `src/app/globals.css` contains `--transition-normal: 250ms ease`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "color-velocity-black" src/app/globals.css | grep -q "^[1-9]" && grep -c "font-montserrat" src/app/globals.css | grep -q "^[1-9]" && ! grep -q "prefers-color-scheme" src/app/globals.css && ! grep -q "font-geist" src/app/globals.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>globals.css contains all Velocity brand tokens from identity doc section 4, @theme block creates Tailwind utility classes for all velocity colors, @theme inline maps fonts, shadcn variables mapped to brand colors, no dark mode media query</done>
</task>

<task type="auto">
  <name>Task 2: Swap Geist fonts for Montserrat + Roboto in root layout</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current state — Geist Sans + Geist Mono imports)
    - docs/VELOCITY-UI-IDENTITY-SYSTEM.md (section 2: Typography — font families and weights)
  </read_first>
  <action>
Modify `src/app/layout.tsx` to replace Geist fonts with Velocity brand fonts per D-03:

1. **Remove imports:** Delete `import { Geist, Geist_Mono } from 'next/font/google'` and both `const geistSans` and `const geistMono` declarations.

2. **Add imports:**
   ```tsx
   import { Montserrat, Roboto } from 'next/font/google'
   ```

3. **Define font instances:**
   ```tsx
   const montserrat = Montserrat({
     subsets: ['latin'],
     variable: '--font-montserrat',
     display: 'swap',
     weight: ['600', '700'],
   })

   const roboto = Roboto({
     subsets: ['latin'],
     variable: '--font-roboto',
     display: 'swap',
     weight: ['400', '500'],
   })
   ```
   - Montserrat weights 600 (heading-sm, heading-md, overline) and 700 (heading-lg, display-md, display-lg) per identity doc section 2.2
   - Roboto weights 400 (body text) and 500 (active sidebar item, badge captions) per identity doc section 2.2

4. **Update html className:**
   Replace `${geistSans.variable} ${geistMono.variable}` with `${montserrat.variable} ${roboto.variable}`.

5. **Keep everything else unchanged:** Metadata, lang="pt-BR", body className.

Final layout.tsx structure:
```tsx
import type { Metadata } from 'next'
import { Montserrat, Roboto } from 'next/font/google'
import './globals.css'

const montserrat = Montserrat({
  subsets: ['latin'],
  variable: '--font-montserrat',
  display: 'swap',
  weight: ['600', '700'],
})

const roboto = Roboto({
  subsets: ['latin'],
  variable: '--font-roboto',
  display: 'swap',
  weight: ['400', '500'],
})

export const metadata: Metadata = {
  title: 'Agency OS',
  description: 'Operating system for a solo-operated marketing agency',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html
      lang="pt-BR"
      className={`${montserrat.variable} ${roboto.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  )
}
```
  </action>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `import { Montserrat, Roboto } from 'next/font/google'`
    - `src/app/layout.tsx` contains `variable: '--font-montserrat'`
    - `src/app/layout.tsx` contains `variable: '--font-roboto'`
    - `src/app/layout.tsx` contains `weight: ['600', '700']` for Montserrat
    - `src/app/layout.tsx` contains `weight: ['400', '500']` for Roboto
    - `src/app/layout.tsx` does NOT contain `Geist`
    - `src/app/layout.tsx` does NOT contain `geistSans`
    - `src/app/layout.tsx` does NOT contain `geistMono`
    - `src/app/layout.tsx` contains `lang="pt-BR"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Montserrat" src/app/layout.tsx && grep -q "Roboto" src/app/layout.tsx && grep -q "font-montserrat" src/app/layout.tsx && grep -q "font-roboto" src/app/layout.tsx && ! grep -q "Geist" src/app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Root layout loads Montserrat (600, 700) and Roboto (400, 500) via next/font/google with CSS variables, Geist fonts fully removed, no layout shift on load</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies CSS tokens and font loading only — no data flow, no user input, no API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | CSS custom properties | accept | CSS variables are client-visible by design — no secrets in color/font tokens |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. All Velocity color tokens available as Tailwind utilities (grep globals.css for @theme block)
3. Font CSS variables present on html element (inspect in browser)
4. No references to Geist fonts remain in codebase root layout
</verification>

<success_criteria>
- App builds successfully with new brand tokens
- Tailwind utilities like `bg-velocity-black`, `text-velocity-lime`, `font-heading`, `font-body` are available
- Body text renders in Roboto, headings can use Montserrat
- shadcn components automatically use brand colors (--background, --foreground, --primary mapped to Velocity tokens)
- No visual regression on existing pages (colors shift from zinc to Velocity palette as expected)
</success_criteria>

<output>
After completion, create `.planning/phases/16-brand-identity-sidebar-layout/16-01-SUMMARY.md`
</output>
